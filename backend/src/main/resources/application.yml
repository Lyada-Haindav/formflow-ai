server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/form_weaver?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: hibernate
    password: haindav@1
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: info
